spring.datasource.url=jdbc:mysql://localhost:3306/fungiflow_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=sandali
spring.datasource.password=group1fungi
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Let Hibernate create the schema fresh at startup
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.sql.init.mode=never

# We?re not specifying a custom platform or data-locations,
# Spring Boot will pick up classpath:data.sql by default
# (No need for spring.sql.init.data-locations unless you have multiple scripts)
